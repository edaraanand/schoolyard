


Please click the link to get an approval or rejection from the school

<a href="http://localhost:4000 <%= url(:registration_last, :id => @parent.id) %>">http://localhost:4000<%= url(:registration_last, :id => @parent.id) %> </a>
