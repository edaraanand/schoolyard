<%= error_messages_for @announcement %>
<h2 class="current-page">From the Principal &gt; Add New</h2>
  <%= form_for(@announcement, :action => url(:from_principals), :id => 'announcement') do %>
    <%= partial 'form' %>
  <% end =%>
