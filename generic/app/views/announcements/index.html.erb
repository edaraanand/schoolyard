
<div class = 'formcontainer'>
          
  <%= form_for(@announcement, :action => url(:controller => 'announcements', :action => 'index'), :id => 'announcement') do %>
         <table>
                <tr>
                    <td> <h3>Announcements</h3></td> </br> 
	            <td> <%= link_to "Add New", resource(:announcements, :new) %></td>
                </tr> 
		<tr>
		    <td><%= select :name => "access_name", :collection => classrooms %></td>
		    <td><%= submit ">>" %></td>
	        </tr>
	 </table>	
  <% end =%> 	    
        <table>
	
	      <% for announcement in @announcements %>
    
                   <tr>
		        <% unless announcement.expiration.nil? %>
                        <td><%= announcement.expiration.strftime("%B-%d") %></td>
			<% end %>
                        <td><%= announcement.title %></td>
			
			  
                   </tr> 
                   <tr>
                        <td><%= announcement.content %></td>
			<td><%= link_to "Edit", resource(announcement, :edit) %></td>
			<td><%= link_to "Delete", resource(announcement, :delete) %></td>
                   </tr>

	      <% end %>
    
     
</div>
 