<% for principal in @from_principals %>
       <div id="sidebar">
            <% if principal.expiration.nil? %>
                  <div class="data-block long-block">
                         <h3><%= principal.title %></h3>
                         <p class="post-content-sidebar"><%= principal.content %></p>
                  </div>
            <% elsif principal.expiration >= @date %>
                  <div class="data-block long-block">
                       <h3><%= principal.title %></h3>
                       <p class="post-content-sidebar"><%= principal.content %></p>
                  </div>
	           <% end %>
               <div class="hrule clear"><hr /></div>
        </div>
<% end %>
       <%= link_to "All Articles", url(:principal_articles, :label => 'principal')  %> <br/>

<h3 class="current-page">
  
      <% unless @welcome_messages.empty? %>
          <% for message in @welcome_messages %>
               <p class="post-content"><%= message.content %></p>
           <% end %>
      <% end %>
  </h3>
 
   <% for announcement in @announcements %>
           <% if announcement.expiration.nil? %>
                  <div class="data-block long-block">
                          <h3><%= announcement.title %></h3>
                          <p class="post-content"><%= announcement.content %></p>
                          <% @attachment = Attachment.find(:first, :conditions => ['attachable_id=?', announcement.id] ) %>
                          <% unless @attachment.nil? %>
                             <p><%= link_to "#{@attachment.filename}", url(:download, :id => @attachment.id) %></p>
                          <% end %>
                   </div>
           <% elsif announcement.expiration >= @date %>
                   <div class="data-block long-block">
                          <h3><%= announcement.title %></h3>
                          <p class="post-content"><%= announcement.content %></p>
                          <% @attachment = Attachment.find(:first, :conditions => ['attachable_id=?', announcement.id] ) %>
                          <% unless @attachment.nil? %>
                             <p><%= link_to "#{@attachment.filename}", url(:download, :id => @attachment.id) %></p>
                          <% end %>
                   </div>
            <% end %>      
            <div class="hrule clear"><hr /></div>
   <% end %>
 
       <%= link_to "All Announcements", url(:principal_articles, :label => 'announcements')  %> <br/>
       
       <% unless @classrooms.empty? %>
             <h4 class="current-page">Quick Links </h4>
             <% @classrooms.each do |f| %>
                 <%= link_to "#{f.class_name}", url(:class_details, :id => f.id) %>
             <% end %>
       <% end %>
     
      <h4 class="current-page">
         External Links </h4>
      <ul> 
       <% for link in @external_links %>
           <li><%= link_to ("#{link.title}", "http://#{link.url}", {'target' => '_blank'}) %></li>
       <% end %>
    </ul>

